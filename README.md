# ChatQuestBot 🎮

Telegram-бот для геймификации чата с ежедневными заданиями и системой баллов.

## 📋 Описание

ChatQuestBot повышает активность участников чата через:
- Ежедневные задания (текст, фото, видео)
- Систему баллов за выполнение заданий и активность в чате
- Еженедельные итоги с определением победителей
- Систему предупреждений и модерации

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd she_codes_bot
```

### 2. Создайте виртуальное окружение\

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте конфигурацию

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и укажите:

```env
BOT_TOKEN=ваш_токен_бота
CHAT_ID=id_вашего_чата
OPERATOR_IDS=123456789,987654321
```

#### Как получить необходимые данные:

1. **BOT_TOKEN**:
   - Создайте бота через [@BotFather](https://t.me/BotFather)
   - Используйте команду `/newbot`
   - Скопируйте полученный токен

2. **CHAT_ID**:
   - Добавьте бота [@userinfobot](https://t.me/userinfobot) в ваш чат
   - Он покажет ID чата (отрицательное число для групп)

3. **OPERATOR_IDS**:
   - Ваш личный ID можно узнать у [@userinfobot](https://t.me/userinfobot)
   - Перечислите ID операторов через запятую

### 5. Запустите бота

```bash
python main.py
```

## 📁 Структура проекта

```
she_codes_bot/
├── bot/
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── user.py          # Обработчики для пользователей
│   │   └── operator.py      # Обработчики для операторов
│   └── utils/
│       ├── __init__.py
│       └── scheduler.py     # Планировщик заданий
├── data/
│   ├── tasks.json           # База заданий
│   └── bot.db               # База данных SQLite (создается автоматически)
├── config.py                # Конфигурация
├── database.py              # Модуль работы с БД
├── main.py                  # Точка входа
├── requirements.txt
├── .env.example
├── .env                     # Ваши настройки (не коммитится)
└── README.md
```

## 🎯 Команды бота

### Для всех пользователей:

- `/start` - Начать работу с ботом
- `/help` - Справка по боту
- `/my_points` - Посмотреть свои баллы
- `/top` - Топ-10 участников недели

### Для операторов:

- `/stats` - Статистика всех участников
- `/warn @username [причина]` - Выдать предупреждение
- `/send_task` - Отправить задание вручную
- `/week_end` - Вручную подвести итоги недели

## 💰 Система баллов

| Действие | Баллы |
|----------|-------|
| Текстовый ответ (≥10 символов) | 100 |
| Фото | 200 |
| Видео | 300 |
| Активность в чате (за слово) | 1 |

**Ограничения:**
- Минимум 10 символов в сообщении
- Максимум 200 баллов за активность в день

## ⚠️ Система предупреждений

- 3 предупреждения = исключение из геймификации
- Операторы выдают предупреждения командой `/warn`
- Пользователь получает уведомление о каждом предупреждении

## 🗓️ Расписание

По умолчанию:
- Задания отправляются в 10:00 и 18:00
- Итоги недели - воскресенье в 20:00

Настраивается в `.env` файле:

```env
TASK_SCHEDULE_TIMES=10:00,18:00
WEEK_END_DAY=6
WEEK_END_TIME=20:00
```

## 🔧 Настройка заданий

Задания хранятся в `data/tasks.json`. Формат:

```json
{
  "text": "Текст задания",
  "content_type": "text|photo|video|mixed",
  "points": 100
}
```

При первом запуске бот автоматически загрузит задания в базу данных.

## 📊 База данных

Бот использует SQLite. Структура таблиц:
- `users` - Пользователи
- `tasks` - Задания
- `daily_tasks` - Отправленные задания
- `answers` - Ответы пользователей
- `points` - История начисления баллов
- `chat_activity` - Активность в чате
- `warnings` - Предупреждения

## 🐛 Логи

Логи сохраняются в файл `bot.log` и выводятся в консоль.

## 🔒 Безопасность

- Не коммитьте `.env` файл (уже добавлен в `.gitignore`)
- Храните токен бота в безопасности
- Регулярно делайте бэкапы базы данных `data/bot.db`

## 📝 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в `bot.log`
2. Убедитесь, что все настройки в `.env` корректны
3. Проверьте, что бот добавлен в чат с правами администратора

---

**Сделано с ❤️ для She Codes**